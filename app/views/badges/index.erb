<h3><%= t('.header') %></h3>

<%= render 'shared/errors', resource: @badge %>

<div class="container">
  <table class="table mt-5">
    <thead>
    <tr>
      <th><%= t('.badge') %></th>
      <th><%= t('.badge_title') %></th>
      <th ><%= t('.badge_rule') %></th>
      <th ><%= t('.actions') %></th>
    </tr>
    </thead>

    <tbody>
    <%= render @badges %>
    </tbody>
  </table>
</div>