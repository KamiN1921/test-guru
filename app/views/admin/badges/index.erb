<h3 class = "mt-5"><%= t('.header') %></h3>

<div>
  <%= link_to t('.new_badge'), new_admin_badge_path%>
</div>

<%= render 'shared/errors', resource: @badge %>

<div class="container">
  <table class="table mt-5">
    <thead>
    <tr>
      <th><%= t('.badge') %></th>
      <th><%= t('.badge_title') %></th>
      <th><%= t('.badge_rule') %></th>
      <th><%= t('.actions') %></th>
    </tr>
    </thead>

    <tbody>
    <%= render @badges %>
    </tbody>
  </table>
</div>