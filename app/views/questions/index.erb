<h1>Questions of Test n<%=@test.id %></h1>
<ul>
<%@questions.each do |question| %>
  <li><%= "ID #{ question.id}: #{question.body}"%></li>
    <%= button_to "Delete", question_path(question.id), method: :delete %>
<% end %>
</ul>