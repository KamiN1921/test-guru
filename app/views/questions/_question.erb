<tr>
  <td> <%= "ID #{ question.id}: #{question.body}"%></td>
  <td> <%= button_to "Delete", question_path(question.id), method: :delete %><td/>
  <td> <%= link_to "Edit", edit_question_path(question.id) %></td>
</tr>